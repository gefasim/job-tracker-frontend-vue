export enum JobApplicationTabEnum {
  JobInfo = 'Job Info',
  Notes = 'Notes',
  Contacts = 'Contacts',
  Documents = 'Documents',
  Company = 'Company',
}

export type JobApplicationTabType = `${JobApplicationTabEnum}`
